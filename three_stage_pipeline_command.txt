cd /abs/path/to/Qwen-PC/ECCV

# 建议：把 key 放在环境变量里（不要写进文件/代码）
export OPENAI_API_KEY="你的key"   # 或 export API_KEY="你的key"

# 运行三阶段管线
python3 three_stage/pipeline.py \
  --input-video-dir /abs/path/to/videos \
  --output-root "$(pwd)/long_three_stage_out" \
  --stages 1,2,3 \
  --overwrite \
  --ffmpeg-bin ffmpeg \
  --post-validate \
  --continue-on-error \
  --api-base "http://model.mify.ai.srv/v1" \
  --provider "volcengine_maas" \
  --model "doubao-seed-1-8-251228"

# 备注：
# - 想断点重续时：去掉 --overwrite，保持同一个 --output-root 再次运行即可。
# - --output-root 可改成你希望的输出目录（建议用新目录从零开始）。
